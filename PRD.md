# PRD · 英语四六级复习 App（本地版 v1）

> 版本：v1（第一版） · 日期：2026-09-03 · 状态：已确认，作为后续开发依据
> 设计原型：`index.html`（同目录，可点击演示页面结构与交互，逻辑为简版）
> 已确认关键决策：① 词库用户自己导入 ② 复习用遗忘曲线（SRS）排程 ③ 考试题用选择题 CSV 批量导入

---

## 1. 产品目标和使用场景

**目标**
做一个**只在本人电脑浏览器里本地运行**的英语四六级复习 App，帮用户高效背单词、刷自导入的考试题，并通过遗忘曲线排程与数据统计持续提升。先自用，后续可能分享给他人，但 v1 不做多用户/云。

**使用场景**
- 用户每天打开本地网页 → 看首页今日任务 → 进入背单词 / 做一套题 → 数据自动累积 → 看统计回顾。
- 全程离线、隐私可控、数据完全在本机。

**非目标（明确不做）**
联网、登录、多用户、云同步、部署上线、手机端、商业化。

---

## 2. 整体页面框架和导航结构

**形态**：单页应用（SPA），无刷新切换视图；纯 HTML + CSS + 原生 JS 单文件，无框架、无构建工具、无外部 CDN。

**布局**
- 左侧固定侧边栏导航（6 个模块）。
- 顶部栏：①「四级 / 六级」分段切换（顶层隔离）② 当前日期 ③ 连续打卡天数。

**导航结构（侧边栏）**
| 入口 | 视图 |
|---|---|
| 🏠 首页总览 | home |
| 📅 每日计划 | plan |
| 📚 背单词 | study |
| 🗂 词库导入 | words |
| 📊 数据统计 | stats |
| 📝 题库 | exams |

**模式切换**：四/六级为顶层隔离维度，切换后所有词库、计划、统计、题库均按当前模式显示，互不影响。

---

## 3. 首页的内容结构

- **问候区**：问候语 + 当前模式（四级/六级）+ 日期。
- **4 张指标卡**：
  - 今日新背：`已学 / 上限`
  - 今日复习：算法排定的待复习数量
  - 连续打卡：天数
  - 累计掌握：掌握词数（box ≥ 3）
- **快捷操作区**：开始今日学习 / 导入词库 / 做一套题（按钮跳对应模块）。
- **词库概况卡**：该模式词总数、未学数、待复习数。
- **最近考试卡**：最近 3 套已做套题的名称与正确率。

---

## 4. 每个模块的用途

| 模块 | 用途 |
|---|---|
| 模式切换（四/六级） | 顶层隔离维度；切换后全部数据/进度按模式显示，互不串。 |
| 首页总览 | 总览仪表盘 + 各模块快捷入口，一屏看清今天该干啥。 |
| 每日计划 | 设定每日新词上限，查看算法排定的今日新词与待复习量，启动学习会话。 |
| 背单词 | 核心学习：出词 + 发音 + 四选一，判对错并回写掌握度。 |
| 词库导入 | 下载模板、导入词库 CSV、预览/查看词表（背单词与计划的"原料"）。 |
| 数据统计 | 周/月背词量图与累计指标、考试平均正确率，用于回顾与激励。 |
| 题库 | 导入选择题套题、作答、自动判分、记正答率与每题做后感、回看结果。 |

---

## 5. 每个模块第一版最必要的数据和操作

**模式切换**
- 数据：`S.mode`（`cet4` / `cet6`）。
- 操作：点击切换 → 刷新全部视图为对应模式数据。

**首页总览**
- 数据：派生自 `words / stats / exams`（见 §7 结构）。
- 操作：展示指标；点击快捷按钮跳转模块。

**每日计划**
- 数据：`settings.dailyNew`（每日新词上限）、未学词数 `unseenCount`、待复习数 `dueCount`。
- 操作：设置并保存每日新词上限；点击"开始今日学习"进入背单词。

**背单词**
- 数据：`word = { id, word, phonetic, meaning, box, next, reviews, correct, wrong }`
  - `box`：0=未学，1–5=SRS 等级；`next`：下次复习时间戳。
- 操作：开始会话 → 显示单词 + 🔊发音 → 四选一 → 即时判对错 → 更新 SRS（答对升 box / 答错回 box1）→ 下一题 → 会话结算（新学/复习/正确率）→ 更新统计与打卡。

**词库导入**
- 数据：`words[mode]` 列表。
- 操作：下载模板（列 `word,phonetic,meaning`）→ 导入 CSV 解析入库 → 预览词表（单词/音标/释义/掌握度/下次复习）。

**数据统计**
- 数据：`stats[mode] = { date, news, reviews }`；考试结果取自 `exams[mode][].result`。
- 操作：查看近 7 天 / 近 30 天柱状图（新词 vs 复习）；查看累计学习、累计复习、掌握词数、考试均正确率。

**题库**
- 数据：`exam = { id, name, questions:[{ stem, opts:[A,B,C,D], answer, explanation }], result:{ date, score, total, accuracy, reflections:{ qid: text } } }`
- 操作：导入套题（CSV：列 `stem,optionA,optionB,optionC,optionD,answer,explanation`，`answer` 为 A–D）→ 开始作答 → 交卷自动判分 → 显示分数/正答率 → 每题可写"做后感"并自动保存 → 可回看结果。

---

## 6. 首页、今日计划和各模块之间的关系

- **首页 = 总入口与仪表盘**：聚合"今日计划"的产出数据（今日新背/复习、连击、累计掌握）并提供到各模块的快捷跳转；不单独产生数据。
- **每日计划 = "今天学什么"的定义者**：由"每日新词上限 + SRS 排程"得到今日新词队列与待复习队列，是**背单词会话的输入来源**。
- **背单词 = 计划的消费者与数据生产者**：消费今日计划队列，产出每个词的掌握度变化、当日统计（news/reviews）、打卡天数。
- **词库导入 = 原料供给**：为背单词、每日计划、数据统计提供词数据；没有词，后面三者无法运行。
- **题库 = 相对独立的另一条线**：与单词流解耦，但其结果会回显到首页"最近考试"与统计"考试均正确率"。
- **数据统计 = 汇总看板**：汇总背单词（stats）与题库（result）的产出，供回顾。

```
词库导入 ──▶ 每日计划 ──▶ 背单词 ──▶ 数据统计
                │            │            ▲
                └────────────┴── 掌握度 ──┘
题库 ──▶ 作答/判分 ──▶ 首页"最近考试" + 统计"考试均正确率"
首页：聚合计划+统计+考试，提供全部快捷入口
```

---

## 7. 本地数据文件、备份和恢复要求

**存储机制**
- 使用浏览器 `localStorage`（无需服务器、双击 html 即可），键名 `cet46_proto_v1`。
- 存一份 JSON：`{ mode, settings:{dailyNew}, streak, lastStudy, words:{cet4:[],cet6:[]}, stats:{cet4:[],cet6:[]}, exams:{cet4:[],cet6:[]} }`。
- 每次关键操作后 `save()`（用 try/catch 包裹，失败降级为内存态不报错）。

**数据位置（供排查）**：浏览器开发者工具 → Application → Local Storage → 对应文件 origin。

**备份（本地文件）**
- 提供「导出备份」：下载 `cet46-backup-YYYYMMDD.json`，包含全部 state（两模式词库、统计、题库、设置、打卡）。
- 位置：在「词库导入」页底部增加"数据备份与恢复"区。

**恢复**
- 提供「导入备份」：选择 JSON 文件 → 确认后覆盖本地 state → 刷新全部视图。
- 导入前必须弹出确认提示，避免误覆盖。

**限制与提醒**
- `localStorage` 与"当前浏览器 + 文件 origin"绑定；换浏览器、清缓存、重装系统会丢失。
- 因此"定期导出 JSON"是 v1 唯一可靠的备份手段，首次导出时应在界面提示用户。

---

## 8. 第一版必须完成的功能

1. 单文件本地网页，双击即可在浏览器打开；**无任何联网请求**（不引 CDN/外部库）。
2. 四/六级模式切换，且两模式数据完全隔离。
3. 词库导入：模板下载 + CSV 解析入库 + 词表预览（列 `word,phonetic,meaning`）。
4. 每日计划：每日新词上限设置 + 简版 SRS（Leitner 5 级）排程出今日新词/待复习队列。
5. 背单词闯关：出词 + 离线发音（Web Speech API）+ 四选一（1 正确 + 3 个同级别错误释义干扰）+ 即时判对错 + 回写掌握度 + 会话结算（新学/复习/正确率）+ 打卡。
6. 数据统计：近 7 天 / 近 30 天背词量 SVG 柱状图（新词 vs 复习）+ 累计指标 + 考试均正确率。
7. 题库：选择题 CSV 导入（列 `stem,optionA–D,answer,explanation`）+ 作答 + 自动判分 + 正答率 + 每题做后感（自动保存）+ 结果回看。
8. 首页总览：4 指标卡 + 快捷入口 + 词库概况 + 最近考试。
9. 数据持久化：刷新 / 关闭 / 重启浏览器后数据不丢。
10. 数据备份与恢复：导出全部数据为本地 JSON，导入 JSON 覆盖恢复（导入前确认）。

---

## 9. 第一版暂时不做的功能

- 登录、多用户、云同步、部署上线、手机端。
- 非选择题题型（填空、作文、听力等）—— v1 只做选择题。
- 内置标准四/六级词表、词库自动校对（用户自己导入，导入错误由用户负责）。
- 排行榜、社交分享、导出分享链接。
- 高级分析（按词根、按错误类型细分、趋势预测等）—— v1 仅基础图表。
- 发音在"不支持 Web Speech API"的浏览器下的音频文件兜底（v1 默认 Chrome/Edge）。
- 词库/题库的批量编辑、删除、合并、去重等高级管理。
- 完整 SM-2 算法（v1 用简化 Leitner 5 级盒子）。
- 多套发音引擎切换、深色/浅色主题切换（v1 固定深色）。

---

## 10. 可以实际检查的验收标准

- **AC1 离线**：双击 `index.html` 在 Chrome/Edge 打开，DevTools → Network 中无任何外部网络请求。
- **AC2 模式隔离**：导入四级词库并学习后，切到六级，词库/计划/统计/题库均为空或各自数据，不串。
- **AC3 词库导入**：下载模板 → 填 ≥10 词 → 导入，词表正确显示，未学数同步增加。
- **AC4 计划队列**：设置每日新词上限 = N，开始学习后当日新词数 ≤ N；待复习数 = 到期（next ≤ 今日）词数。
- **AC5 背单词交互**：出示单词 + 点击 🔊 有英文发音；四选一含 1 正确 + 3 干扰（干扰释义来自同级别已导入词）；选对高亮绿、选错高亮红并显示正确项。
- **AC6 SRS 与结算**：答对 box 升、答错回 box1；会话结束显示新学/复习/正确率；当日 `stats` 被写入。
- **AC7 持久化**：关闭并重新打开浏览器，词、进度、统计、考试结果均在。
- **AC8 统计图表**：近 7 / 30 天柱状图正确反映每日 news/reviews；累计学习、复习、掌握、考试均正确率数值正确。
- **AC9 题库闭环**：导入一套选择题 → 作答交卷 → 分数与正答率正确 → 每题可做后感并保存 → 「查看」可回看题目、答案、解析、做后感。
- **AC10 备份恢复**：导出 JSON → 清空 localStorage → 导入该 JSON → 全部数据完整恢复，与导出前一致。
- **AC11 首页聚合**：首页今日新背/复习、连击、累计掌握、最近考试与底层数据一致。

---

*说明：本 PRD 与同目录 `index.html` 原型配套；原型用于确认页面结构与交互，v1 实现以本 PRD 为准。*
