# 提示词 · 四六级复习 App UI 全面重设计（Apple 风格）

> 适用对象：本项目（`index.html` + `logic.js` + `default-words.js`）的界面层重构，不涉及业务逻辑重写。

---

## 0. 角色与任务总述

你是一名资深前端设计工程师，精通 Apple 官方设计语言（apple.com 官网视觉 + Human Interface Guidelines），擅长用纯 HTML/CSS/原生 JS 实现高级感的界面、微交互与粒子动效。

你的任务：对这个「英语四六级复习」本地单页应用做**全面的 UI 重设计与排版优化**，包括：
1. 整体布局与视觉风格重构（对标 `https://www.apple.com/cn/iphone/` 的设计语言）；
2. 按钮、卡片、表格、表单、标签等全部组件的重新设计；
3. 文字层级关系与排版重建（字号、字重、行高、留白、信息分组）；
4. 视图转场效果与元素入场动画；
5. 粒子效果（首页氛围背景、学习完成庆祝等）；
6. 微交互（悬停、按压、答对/答错反馈、进度、数字滚动等）。

**侧重：内容展示的清晰度与界面美感。功能逻辑一行都不要破坏。**

---

## 1. 项目现状（必读，先理解再动手）

### 1.1 技术形态
- 单页应用（SPA），视图通过 `innerHTML` 模板字符串无刷新切换：`go(v)` → `render(v)` → `renderHome/renderPlan/renderStudy/renderWords/renderStats/renderExams`。
- 文件结构：`index.html`（内联 CSS + 视图渲染胶水代码）、`logic.js`（暴露 `window.AppLogic`，纯业务逻辑）、`default-words.js`（内置词库数据）。
- 状态存于 `localStorage['cet46_proto_v1']`，关键操作后调用 `save()`。
- 固定深色主题（PRD 明确 v1 不做明暗切换）。
- 已有测试：`tests/` 目录下 `phase1.test.js` ~ `phase11.test.js`，改动后必须全部跑通。

### 1.2 技术红线（任何设计方案都不得违反）
| # | 红线 | 说明 |
|---|---|---|
| R1 | **完全离线** | 禁止任何网络请求：不得引入 CDN、外部字体、外部图标库、外部 JS/CSS、外部图片、外部 lottie/json 资源。DevTools → Network 必须零请求。 |
| R2 | **不引框架/构建工具** | 只允许原生 HTML + CSS + JS。所有效果手写或用内联 SVG / Canvas 实现。 |
| R3 | **保持文件结构** | 继续在 `index.html` 内写样式与模板；`logic.js` 的业务逻辑函数不改（除非有绝对必要并说明理由）；不得改动 `localStorage` 数据结构。 |
| R4 | **功能零回退** | 原 11 条验收标准（见 §13.1）全部保持通过；所有现有按钮的跳转/回调（如 `onclick="go('study')"`）保持可用。 |
| R5 | **性能预算** | 所有动画保持 60fps；粒子系统 Canvas 粒子数 ≤ 80；页面首屏渲染不出现可感知卡顿；`prefers-reduced-motion` 时关闭粒子与大幅动画。 |

### 1.3 现有 UI 问题清单（重设计要逐条解决）
1. **文字没有层级**：标题（`h2.h` 20px）、副标题（`.sub` 13px）、正文（14px）、卡片数字（28px）之间缺乏节奏感，数字不够大、标签不够小，信息权重混乱。
2. **组件粗糙**：按钮只有一种圆角矩形样式（`.btn` 10px 圆角），无按压反馈、无主次体系；卡片（`.card`）千篇一律，无视觉焦点。
3. **emoji 当图标**（🏠📅📚🗂️📊📝🔊🔥）：跨平台渲染不一致，廉价感强，与 Apple 风格严重不符。
4. **导航无动效**：侧边栏切换无过渡、无激活指示动画；顶部分段控件（`.seg`）切换生硬。
5. **视图切换无转场**：`render()` 直接替换 innerHTML，内容瞬间跳变。
6. **答题反馈弱**：答对/答错只有边框变色，无动画、无情绪反馈；学习完成页（`.empty`）简陋，无庆祝感。
7. **表格密集**：词表与题库列表（`table`）行距紧、无斑马纹/悬停态、掌握度标签（`.pill`）不直观。
8. **空状态敷衍**：`.empty` 只是一段灰字加虚线框。
9. **统计图表朴素**：纯 SVG 柱状图无圆角、无入场动画、无图例设计。
10. **进度条细弱**：`.progress` 只有 6px，无动画光效。
11. **整体留白不足**：`--bg:#0f1115` 偏灰，对比层次弱；卡片间距 16px 偏小，内容拥挤。

---

## 2. 设计总方向：Apple 官网设计语言拆解

从 `https://www.apple.com/cn/iphone/` 提炼以下核心特征，全部落到本项目：

1. **极深背景 + 高对比文字**：接近纯黑的背景（#000 ~ #0a0a0c），正文用 Apple 标志性灰白 `#f5f5f7`，次级文字 `#86868b`。靠明度对比而不是边框线来分层。
2. **超大标题 + 短促文案**：官网每个区块都是「小字眉题（eyebrow）→ 超大主标题（常为两行短句）→ 一行副文案 → 双 CTA」的结构。首页问候区与学习完成页要按此结构重排。
3. **数字即主角**：指标数字用超大字号（32–48px）、半粗字重、等宽数字（`font-variant-numeric: tabular-nums`），单位用小号灰字跟在后面。
4. **胶囊按钮**：主按钮为大圆角胶囊形（border-radius: 980px），Apple 蓝 `#0071e3`，悬停提亮、按压缩小 0.97；次级按钮为浅灰底或描边胶囊；文字链用暗色模式蓝 `#2997ff` 加「›」箭头。
5. **大圆角卡片**：卡片圆角 18–28px，背景用比页面背景亮一档的深灰（#161618 / #1c1c1e），重要卡片可加极淡的内发光或渐变描边。
6. **玻璃材质（Glass Material）**：顶栏与侧边栏用 `backdrop-filter: blur() + saturate()` 的半透明磨砂材质，体现 HIG 的 depth 原则。
7. **克制的色彩**：全局几乎无彩色，只在品牌强调（蓝色系）与语义反馈（绿 #30d158 成功 / 红 #ff453a 错误 / 橙 #ff9f0a 警告）上用色。可用一条细腻的蓝紫青渐变（Apple Intelligence 风格）作为品牌装饰线或焦点光晕。
8. **内容呼吸感**：区块间距 32–56px，卡片内边距 24–32px，内容区最大宽度约 1080px 居中，避免拉满全屏的压迫感。
9. **平滑、带惯性的动效**：一切过渡使用 Apple 式缓动（见 §8.2），时长 200–500ms，元素入场带上移 + 淡入 + 轻微交错（stagger）。

---

## 3. 设计令牌（Design Tokens）

把 `:root` 变量整体替换为以下体系（可微调，但保持语义命名与层级）：

```css
:root{
  /* 背景层级：页面 → 表面 → 高亮表面 */
  --bg:#000000;
  --surface:#161618;
  --surface-2:#1c1c1e;
  --surface-3:#2c2c2e;
  /* 分割与描边：用半透明白代替实线 */
  --hairline:rgba(255,255,255,.08);
  --hairline-strong:rgba(255,255,255,.14);
  /* 文字 */
  --text:#f5f5f7;
  --text-secondary:#a1a1a6;
  --text-tertiary:#6e6e73;
  /* 品牌与语义 */
  --accent:#0071e3;         /* 主按钮蓝 */
  --accent-hover:#0077ed;
  --link:#2997ff;           /* 暗色模式文字链蓝 */
  --ok:#30d158;
  --bad:#ff453a;
  --warn:#ff9f0a;
  /* 品牌渐变（装饰用，克制使用） */
  --brand-gradient:linear-gradient(135deg,#0a84ff 0%,#5e5ce6 45%,#bf5af2 100%);
  /* 圆角 */
  --r-sm:10px; --r-md:14px; --r-lg:20px; --r-xl:28px; --r-pill:980px;
  /* 阴影（深色主题以柔和黑影+微光为主） */
  --shadow-1:0 2px 10px rgba(0,0,0,.35);
  --shadow-2:0 12px 32px rgba(0,0,0,.45);
  --glow-blue:0 0 24px rgba(10,132,255,.25);
  /* 玻璃材质 */
  --glass:rgba(22,22,24,.72);
  --glass-blur:blur(20px) saturate(180%);
  /* 动效 */
  --ease-apple:cubic-bezier(.32,.72,0,1);   /* Apple 官方式缓动 */
  --ease-spring:cubic-bezier(.175,.885,.32,1.1);
  --t-fast:.18s; --t-base:.32s; --t-slow:.5s;
}
```

字体栈：
```css
body{
  font-family:-apple-system,BlinkMacSystemFont,"SF Pro Text","SF Pro Display",
    "PingFang SC","Segoe UI","Microsoft YaHei",system-ui,sans-serif;
  -webkit-font-smoothing:antialiased;
  text-rendering:optimizeLegibility;
}
```

---

## 4. 文字层级与排版规范

建立 8 级文字体系，所有视图的文案必须归入其中，禁止随手写 `font-size`：

| 级别 | 用途 | 规格 |
|---|---|---|
| Display | 首页问候语、学习完成页主标题 | 40–48px / 600 / 行高1.15 / 字距 -0.5px |
| H1 | 视图主标题 | 28–32px / 600 |
| H2 | 区块标题（如「数据备份与恢复」） | 20px / 600 |
| Eyebrow 眉题 | 区块上方的分类小字（如「今日概览」「词库管理」） | 12px / 600 / 大写字距 0.08em / 颜色 var(--link) 或三级灰 |
| Stat 数字 | 指标卡数值 | 36–44px / 600 / tabular-nums；单位用 13px 三级灰 |
| Body | 正文、表格内容 | 14–15px / 400 / 行高 1.6 |
| Caption | 辅助说明、`.hint`、时间、音标标注 | 12–13px / 400 / var(--text-tertiary) |
| Label | 按钮文字、表单标签 | 14px / 500 |

排版规则：
1. **信息分组**：每组信息遵循「眉题 → 主信息 → 辅助说明」三段式；组内间距 8–12px，组间间距 24–32px。
2. **数字排版**：所有统计数字启用 `font-variant-numeric:tabular-nums`，配 `count-up` 滚动动画（见 §8.4）。
3. **单词卡文字**：单词本体 56–64px / 700 / 字距 0.5px；音标 18px / var(--text-secondary) / 与单词间距 12px；发音按钮与单词垂直居中排列。
4. **中文文案风格**：学习苹果官网的短句风格——主标题尽量 4–10 字一行、可拆两行；副文案一句话说清价值。例如每日计划页副标题可改为「你定目标，算法排程。」这类表达（可自拟，保持克制、不油腻）。
5. **行宽**：正文段落最大行宽 680px；卡片内文字不贴边，左右留 ≥24px。

---

## 5. 全局框架重设计

### 5.1 布局骨架
- 保持「左侧栏 + 顶栏 + 内容区」结构，但内容区内部加 `max-width:1080px; margin:0 auto; padding:40px 48px;`。
- 侧边栏与顶栏使用玻璃材质（`--glass` + `--glass-blur`），内容区滚动时从下方透过，体现层次。

### 5.2 侧边栏（现 `.sidebar`）
- 宽度 240px；背景 `--glass` + 模糊 + 右侧 1px `--hairline`。
- **品牌区**：去掉彩色圆点，改为 28px 的品牌图形（用内联 SVG 画一个简洁的书本/字母徽标，可用 `--brand-gradient` 填充）+ 名称「46级复习」17px/600；下方 Caption 显示「本地离线 · 数据存本机」。
- **图标**：全部弃用 emoji，换成 **内联 SVG 线性图标**（24px、1.5px 描边、圆角端点，仿 SF Symbols 风格）。需要 6 个：首页（房子）、每日计划（日历）、背单词（书本）、词库导入（文件夹+箭头）、数据统计（柱状图）、题库（文档+笔）。图标颜色随状态变化。
- **导航项**：高度 44px（HIG 最小可点击目标），圆角 12px；默认三级灰；悬停 `--surface-2` + 文字提亮；**激活态**：文字白色 + 图标变蓝 + 左侧 3px 圆角蓝色指示条，且指示条位置切换带 300ms `--ease-apple` 滑动动画（可用一个绝对定位的指示元素跟随激活项移动实现）。
- 底部 `side-foot`：12px 三级灰，可加一个细小的「v1 · 无联网」胶囊标签。

### 5.3 顶栏（现 `.topbar`）
- 高度 56px，玻璃材质 + 底部 1px `--hairline`，sticky 固定。
- 左侧：当前视图标题（17px/600），切换视图时标题做淡入上移过渡。
- 中间：**分段控件重做**——胶囊容器（`--surface-2` + `--hairline` 描边），内部一个白色/深灰的「滑块（thumb）」绝对定位，切换四级/六级时滑块以 `--ease-spring` 400ms 滑动，文字颜色同步过渡。这是 Apple 标志性控件，必须做出滑动感。
- 右侧：日期（13px 二级灰）+ 连续打卡：🔥 换成内联 SVG 火焰图标，数字 15px/600，连续 ≥1 天时火焰带微弱的橙色光晕动画（`filter:drop-shadow` 呼吸）。

---

## 6. 组件规范

### 6.1 按钮（重做 `.btn` 体系）
| 类型 | 样式 |
|---|---|
| `.btn-primary` | 胶囊形；背景 `--accent`；文字白；高 44px；padding 0 22px；14px/500。悬停 `--accent-hover` + 微亮；按下 `transform:scale(.97)` 120ms。主操作每屏最多 1 个。 |
| `.btn-secondary` | 胶囊形；背景 `--surface-3`；文字白；悬停提亮一档。 |
| `.btn-ghost` | 胶囊形；透明底 + 1px `--hairline-strong` 描边；文字白；悬停背景 `rgba(255,255,255,.06)`。 |
| `.btn-danger` | 幽灵按钮样式，但描边与文字用 `--bad` 的 80% 透明度，悬停填充淡红底 `rgba(255,69,58,.12)`。**不要纯红底**，危险操作靠确认弹窗兜底。 |
| `.btn-link` | 文字链：`--link` 色 + 「›」符号，悬停下划线 + 箭头右移 2px。用于「进一步了解」式次要入口。 |
| `.btn-icon` | 44×44 圆形，用于发音、更多操作等图标按钮。 |

所有按钮：`transition:all var(--t-fast) var(--ease-apple)`；`cursor:pointer`；`:focus-visible` 有 2px 蓝色 focus ring（无障碍）。

### 6.2 卡片（重做 `.card`）
- 背景 `--surface`；1px `--hairline` 描边；圆角 `--r-lg`（20px）；内边距 24–28px。
- 悬停（可点击的卡片才加）：`translateY(-2px)` + `--shadow-2` + 描边变 `--hairline-strong`，200ms 过渡。
- **指标卡变体**：顶部一行眉题（12px 三级灰）+ 大号数字（40px/600 tabular）+ 小号单位；数字加载时 count-up 动画。首页 4 张指标卡中，「今日新背」可做成强调卡：背景叠一层 8% 透明度的 `--brand-gradient` 渐变 + 左上角一个小 SVG 装饰。
- **区块眉题**：每张内容卡片内部第一行加眉题（见 §4），替代现在直接写 `.k` 的做法。

### 6.3 标签 `.pill`
- 高度 22px；圆角 999px；12px/500；padding 0 10px。
- 语义色用「底色 12% + 文字 100%」模式：成功 `rgba(48,209,88,.15)`/`#30d158`；警告橙同理；掌握度 L1–L5 用蓝色系深浅渐进（box 越高越亮），让掌握度一眼可辨。

### 6.4 表格（词表、题库列表）
- 去掉竖线与密集横线：只保留行与行之间 1px `--hairline`；表头 12px/600 三级灰、字距 0.04em。
- 行高 52px；行悬停背景 `rgba(255,255,255,.03)`。
- 单词列 15px/500 白色；音标列用斜体感的二级灰；释义列 14px 二级；长文本 `ellipsis` 单行截断 + `title` 属性。
- 表格容器卡片加 `overflow:auto` 与圆角。

### 6.5 表单
- 输入框：高 44px；背景 `--surface-2`；1px `--hairline` 描边；圆角 12px；聚焦时描边变 `--accent` + 2px 外发光 `rgba(0,113,227,.3)`，200ms 过渡。
- 数字输入（每日新词上限）：改成 **步进器样式**——左侧减号圆钮、中间数字、右侧加号圆钮（Apple 式 Stepper），同时保留直接输入；范围 1–200。
- 文件导入按钮：隐藏 `<input type=file>`，用 `.btn-secondary` 触发；导入区可做成一个带虚线描边的「拖拽上传」视觉区（即使暂不支持拖拽，视觉上也给出明确的投放区），区内放图标 + 一行主文案 + 一行 Caption。

### 6.6 Toast
- 从底部居中改为**顶部居中下滑入场**（或保持底部但用弹簧缓动）：胶囊形、玻璃材质、带对勾/警示图标；入场 `--ease-spring`，2.2s 后淡出上移。

### 6.7 空状态
- 每种空状态 = 一个 64px 的内联 SVG 插画（线性风格，如空盒子、书本）+ 一行 17px/600 主文案 + 一行 13px 说明 + 一个主按钮。禁止再出现纯虚线框灰字。
- 文案示例：词库空 →「词库还是空的」/「导入一份词表，马上开始」；今日完成 →「今天的任务，全部完成」/「坚持的样子，很 Apple」（可自拟，保持克制）。

### 6.8 确认弹窗（替换原生 `confirm()`）
- 原生 `confirm` 与整体风格冲突，重做为居中模态框：背景遮罩 `rgba(0,0,0,.5)` + 模糊 4px；卡片宽 320px、圆角 20px、居中；标题 17px/600 + 说明 14px 二级 + 底部两个胶囊按钮（取消幽灵 / 确认主色或红色）。出现用 scale .92→1 + 淡入 240ms，消失反向。用于：清空词库、导入备份覆盖、加载内置词库覆盖。

---

## 7. 模块逐一重设计

### 7.1 首页总览（renderHome）
布局自上而下：
1. **Hero 问候区**：眉题「四级模式 · 2026年9月3日」（小字蓝色）→ Display 级问候「你好，子阳」（48px/600，可随时间段显示早安/午安/晚安）→ 副文案 15px 二级「今天也稳扎稳打。」。背景层放置粒子画布（见 §9.1）。
2. **今日概览**：眉题「今日概览」+ 4 张指标卡（2×2 或 1×4，视宽度），数字 count-up 入场，卡片交错入场（间隔 60ms）。
3. **快捷操作**：改为 3 张「入口卡」而非按钮堆：每张卡 = 内联 SVG 图标 + 标题（开始今日学习 / 导入词库 / 做一套题）+ 一行说明 + 「›」，整卡可点击、悬停上浮。第一张为渐变强调卡。
4. **词库概况 + 最近考试**：两张并排卡片。词库概况用一个大数字（总词数）+ 两个次级指标（未学/待复习）；最近考试每行 = 正确率胶囊 + 套名 + 日期，正确率 ≥60 绿色否则橙色。

### 7.2 每日计划（renderPlan）
- 结构：眉题「每日计划」→ H1「你定目标，算法排程。」→ 副文案一句话说明遗忘曲线。
- 设置卡：步进器样式的新词上限输入 + 保存按钮（保存成功时按钮短暂变绿打勾再复原）。
- 两个统计块（今日新词 / 今日待复习）做成左右两张指标卡，附环形进度（SVG 圆环，显示 已完成/上限 比例，描边用 `--brand-gradient`）。
- 底部主按钮「开始今日学习」（全屏唯一 primary，胶囊，居中，48px 高）。

### 7.3 背单词（renderStudy）——重点打磨
- 顶部：细进度条升级为 6px → **8px 圆角渐变进度条**（`--brand-gradient` 填充 + 微光），进度变化 400ms `--ease-apple` 过渡；进度条上方右侧显示「3 / 20」13px 二级 + 左侧眉题「新词/复习」。
- **单词卡**：页面视觉中心。背景 `--surface` 大圆角 28px、内边距 56px、微妙的顶部渐变光；单词 60px/700；音标 18px 二级；发音按钮为 52px 圆形玻璃按钮，点击时向外扩散 2 圈声波纹动画（CSS keyframes 圆环放大淡出）。
- **选项按钮**：2×2 网格，每项高 ≥56px、圆角 16px、背景 `--surface-2`、1px `--hairline`；悬停上浮 1px + 描边亮蓝；**答对**：该选项背景渐变到绿色 12% + 描边绿 + 卡片轻快弹一下（scale 1.01 回弹）；**答错**：选中项红色 + 左右抖动 300ms（translateX ±6px），同时正确项亮起绿色；其余选项淡出到 40% 透明度。
- 判定后出现的「下一题」按钮：主色胶囊，从下方淡入上移。
- **会话完成页**：全屏仪式感——中央大标题「今日学习完成」（Display 级）+ 触发一次粒子彩带庆祝（见 §9.2）+ 三张统计（新学/复习/正确率）数字 count-up + 主按钮「返回首页」。若正确率 ≥80%，副文案给一句肯定语。
- 词库为空的空状态按 §6.7 处理。

### 7.4 词库导入（renderWords）
- 分为三个视觉区块，各用卡片承载并加眉题：「导入」「内置词库」「危险操作」逐级降低视觉权重，清空词库按钮收进折叠区或放在卡片最底部（降低误触）。
- 导入区做成拖拽上传视觉区（§6.5）。
- 词表统计行：总词数用 24px/600，右侧三个次级指标用胶囊标签展示。
- 词表按 §6.4 规范重做；掌握度胶囊用蓝色渐进色阶。

### 7.5 数据统计（renderStats）
- 顶部 4 张累计指标卡（同首页指标卡规范，数字 count-up）。
- 两张图表卡片：「近 7 天」「近 30 天」。SVG 柱状图重做：柱体圆角 4px、新词用 `--accent`、复习用 `--brand-gradient` 或 40% 白；柱体从底部生长入场（scaleY 0→1，逐柱延迟 30ms）；网格线用 5% 白；图例做成小圆点 + 12px 文字；悬停柱体提亮并用 `<title>` 或浮层显示数值。
- `logic.js` 的 `barChart()` 若不便改，可在胶水层包一层动画类名，不改其输出结构。

### 7.6 题库（renderExams）
- 导入卡：套名输入 + 拖拽上传区（同 §6.5）。
- 套题列表：从表格改为**列表卡片**——每套一行卡片：套名（15px/500）+ 题数/状态胶囊/正确率 + 右侧操作按钮（开始=主色小胶囊、查看=幽灵小胶囊）。已做的卡片左侧加一条 3px 绿色状态条。
- 作答页：每题一张卡，题干 16px/500；选项高 48px、圆角 12px、选中态蓝边 + 蓝色 8% 底；底部悬浮一个「交卷」按钮条（玻璃材质吸底）。
- 交卷后：对错即时着色（同背单词反馈），每题下方展开做后感输入框（圆角、占位文案「这题的易错点…」），自动保存不变；顶部加一张得分卡：大号分数「18/20」+ 正确率环形图（SVG 圆环 + 中心百分比）。
- 结果回看页同风格，解析区块用左侧 3px 蓝色引用条样式。

---

## 8. 转场与动效系统

### 8.1 视图转场
`go(v)` 切换视图时：旧内容 150ms 淡出 + 上移 8px → 渲染 → 新内容 300ms 淡入 + 从下方 16px 上移，缓动 `--ease-apple`。实现方式：给 `#view` 加 `.view-leave`/`.view-enter` 类，用 `transitionend` 或 `setTimeout` 衔接 innerHTML 替换；注意不要阻塞交互（总时长 ≤450ms）。

### 8.2 缓动与时长规范
- 标准缓动 `cubic-bezier(.32,.72,0,1)`（Apple 常用）；弹性用 `cubic-bezier(.175,.885,.32,1.1)`。
- 微交互 120–200ms；组件状态 200–320ms；视图/区块 320–500ms。禁止使用 `linear` 和默认 `ease` 做位移类动画。

### 8.3 元素入场
- 每个视图渲染后，卡片按文档顺序交错入场：`opacity:0; transform:translateY(16px)` → 正常态，每项延迟 40–60ms，用 `animation` 或渲染后 `requestAnimationFrame` 加类触发。
- 因为视图靠 innerHTML 重建，入场动画只在视图切换时触发一次，不要影响视图内部的输入交互（如做后感输入不会触发重渲染，安全）。

### 8.4 数字滚动（count-up）
- 所有指标数字从 0 滚动到目标值，500–800ms，`--ease-apple` 减速曲线；用 `requestAnimationFrame` 实现一个小工具函数 `countUp(el, target)`。

### 8.5 必须避免
- 任何布局抖动（动画只用 transform/opacity）；
- 长时间循环动画常驻（呼吸类动画限制在火焰、粒子等小元素上）；
- 阻塞式动画（动画期间按钮必须可点）。

---

## 9. 粒子效果

全部用**内联 Canvas + 原生 JS**实现（离线红线），封装为单一粒子模块，支持全局开关与 `prefers-reduced-motion` 自动关闭。

### 9.1 首页氛围粒子
- 位置：首页 Hero 区背景（画布铺满内容区顶部约 400px 高度，置于内容之下）。
- 效果：30–50 个微光点，蓝/紫/青三色（取自 `--brand-gradient`），半径 1–3px，透明度 0.1–0.35，以 0.1–0.3px/帧 的速度缓慢上浮并轻微左右漂移，超出边界后从底部重生；可加 2–3 个大半径（60–120px）低透明度（0.04）的模糊光斑缓慢移动，营造 Apple 发布会式氛围光。
- 交互：鼠标在 Hero 区移动时，粒子以极弱力度（影响半径 120px）避让或吸引，速度增量 ≤0.2，保持整体静谧。

### 9.2 学习完成彩带/粒子庆祝
- 触发：`finishSession()` 渲染完成页时播放一次。
- 效果：从屏幕中上部爆发 60–80 个粒子（小矩形 + 圆点混合），颜色取品牌渐变 5 色，带重力（0.08–0.15/帧）与空气阻力（0.98），初速度随机、带旋转，1.8–2.5s 内全部落下淡出后自动清理画布并停止 rAF。
- 若正确率 ≥90%，追加第二波小规模爆发。

### 9.3 打卡火焰微光（可选增强）
- 顶栏打卡数字旁，用 CSS `drop-shadow` 呼吸光晕即可，不必用 Canvas。

### 9.4 粒子系统硬性要求
1. 单画布粒子数上限 80；总 rAF 循环在页面不可见时（`document.visibilitychange`）暂停。
2. 按 `devicePixelRatio` 缩放画布，避免模糊。
3. `prefers-reduced-motion: reduce` 时完全禁用粒子与庆祝动画。
4. 粒子画布 `pointer-events:none`，不挡交互。

---

## 10. 微交互清单（逐项落实）

| 元素 | 微交互 |
|---|---|
| 侧边导航 | 悬停底色 120ms；激活指示条 300ms 滑动；点击 0.98 缩放 |
| 分段控件 | 滑块 400ms 弹簧滑动 |
| 所有按钮 | 悬停提亮；按下 scale .97；focus-visible 蓝色描边 |
| 指标卡 | 数字 count-up；可点击卡悬停上浮 2px + 阴影 |
| 单词卡发音钮 | 点击声波扩散 2 环；播放中图标微脉动 |
| 答题选项 | 悬停上浮；答对绿色弹跳；答错红色抖动；其余淡出 |
| 进度条 | 宽度 400ms 过渡 + 渐变微光 |
| 保存按钮 | 成功瞬间变绿 + 打勾 600ms 后复原 |
| 表格行 | 悬停底色；操作按钮悬停才完全显现（默认 70% 透明度） |
| Toast | 弹簧入场 + 定时淡出上移 |
| 模态框 | 遮罩淡入 + 卡片 scale 92%→100% |
| 模式切换 | 切换后当前视图内容做一次轻量淡入（提示数据已刷新） |

---

## 11. 无障碍与性能验收

1. **对比度**：正文与背景对比度 ≥ 4.5:1（`#86868b` 只用于 ≥13px 的辅助文字，关键信息用 `#f5f5f7`）。
2. **键盘可达**：所有按钮/选项可用 Tab 聚焦、Enter 触发；`:focus-visible` 样式完整。
3. **动效偏好**：`prefers-reduced-motion` 时关闭粒子、入场交错与抖动动画，保留即时状态变化。
4. **性能**：动画只用 `transform/opacity`；粒子按 §9.4 执行；切换视图无内存泄漏（粒子画布随视图销毁时停止 rAF 并移除）。
5. **响应式底线**：窗口宽度 900–1920px 内布局不破：指标卡 4 列 → 2 列自适应，内容区留白收窄；侧边栏在 <1000px 时可收缩为 72px 图标栏（图标居中、悬停出 tooltip）。

---

## 12. 实施步骤（分阶段，每步自验）

1. **阶段 A · 令牌与全局框架**：替换 `:root` 与字体；重做侧边栏（SVG 图标 + 激活动画）与顶栏（玻璃材质 + 分段滑块）；内容区限宽与留白。自验：六视图打开无样式破损，`tests/` 全绿。
2. **阶段 B · 组件体系**：按钮/卡片/胶囊/表格/表单/Toast/空状态/模态框全部替换，替换所有 `confirm()`。自验：导入、清空、备份恢复流程可用。
3. **阶段 C · 文字排版与视图重构**：按 §4、§7 逐视图重排（首页 → 计划 → 背单词 → 词库 → 统计 → 题库）。自验：11 条原验收逐条走查。
4. **阶段 D · 动效系统**：视图转场、入场交错、数字滚动、答题反馈动画。自验：快速连续切换视图不闪烁不卡。
5. **阶段 E · 粒子系统**：首页氛围粒子 + 完成庆祝，接好可见性暂停与 reduced-motion 开关。自验：DevTools Performance 录制 5s，帧率 ≥55fps。
6. **阶段 F · 收尾**：清理无用旧样式；统一命名；跑全部测试；DevTools Network 确认零请求；输出一份简短的改动说明。

---

## 13. 最终验收清单

### 13.1 原有功能验收（必须全部保持通过）
- AC1 双击打开、无任何外部网络请求
- AC2 四/六级数据完全隔离
- AC3 词库模板下载 + 导入正确
- AC4 每日上限与待复习队列正确
- AC5 背单词发音 + 四选一判对错
- AC6 SRS 升降级 + 会话结算 + 统计写入
- AC7 刷新/关闭/重启数据不丢
- AC8 周/月图表与累计指标正确
- AC9 题库导入→判分→做后感→回看
- AC10 导出/导入 JSON 备份恢复
- AC11 首页聚合与底层数据一致
- 另：`tests/` 目录全部测试通过。

### 13.2 新增视觉/交互验收
- [ ] 视觉对标：整体观感接近 apple.com/cn/iphone 暗色区块（深黑背景、大号留白、胶囊按钮、玻璃导航）。
- [ ] 全站无 emoji 图标（除文案语气用途外），全部为内联 SVG。
- [ ] 文字层级符合 §4 八级体系，随机抽 3 个视图检查。
- [ ] 视图切换有转场；卡片有交错入场；指标数字有滚动动画。
- [ ] 分段控件、导航指示条有滑动动画。
- [ ] 答题对错有弹跳/抖动 + 颜色反馈；完成页有粒子庆祝。
- [ ] `prefers-reduced-motion` 下粒子与大幅动画关闭。
- [ ] Performance 面板无明显掉帧；Network 面板零请求。

---

## 14. 参考资料使用指引

以下资料用于理解风格与提取灵感。**注意：本项目禁止加载任何外部资源，所有效果必须离线手写实现；若你的运行环境无法联网，直接以本文档 §2–§11 的规范为准。**

| 来源 | 用途 |
|---|---|
| `https://www.apple.com/cn/iphone/` | 总体视觉基调：深黑背景、超大标题、短文案、胶囊按钮、卡片式区块、明暗对比。重点观察其每个区块的「眉题→标题→副文案→双 CTA」结构与留白节奏 |
| `https://developer.apple.com/design/human-interface-guidelines` | 设计原则（Clarity 清晰 / Deference 顺从内容 / Depth 层次）；布局与排版章节的层级思想；44pt 最小可点击目标；对比度要求。注意 HIG 面向原生应用，本项目取其原则而非照搬控件规格 |
| `https://developer.apple.com/design/resources/` | SF Symbols 图标风格参考：用内联 SVG 仿制其线性图标的笔画粗细（1.5px）、圆角端点与 24px 网格 |
| `https://www.hyperui.dev/` | 组件排版参考：统计卡、列表、空状态的布局密度与间距处理（只学排版，不引入其 Tailwind 代码） |
| `https://flowbite.com/blocks/` | 区块组合参考：仪表盘指标区、表格、表单区块的结构划分（只学结构） |
| `https://lottiefiles.com/` | 动效节奏参考：成功庆祝、进度、加载类动画的时长与缓动感觉。**禁止引入 lottie 播放器或 JSON 文件**，用 Canvas/CSS 实现等效效果 |
| `https://godly.website/` | 高端网页视觉参考：氛围光、渐变、大字排版的运用分寸 |
| `https://www.awwwards.com/websites/micro-interaction/` | 微交互灵感：悬停、按压、磁性按钮等；本项目只取其中可用纯 CSS 实现的轻量效果 |

---

## 15. 禁止事项（负面清单）

1. 禁止引入任何 CDN、npm 包、框架、字体文件、图标库、图片资源、lottie 文件。
2. 禁止改动 `localStorage` 键名、数据结构与 `logic.js` 的导出接口。
3. 禁止为了视觉效果删除或合并现有功能入口（6 个导航、备份恢复、模板下载、清空词库等全部保留）。
4. 禁止使用 `alert()`/`confirm()`/`prompt()`（统一换成自绘模态框）。
5. 禁止动画阻塞交互、禁止常驻高耗 rAF 循环。
6. 禁止使用亮色主题或全局彩色背景——克制的深色是本项目风格底线。
7. 禁止在没有跑通 `tests/` 与 §13.1 的情况下宣布完成。

---

*交付标准：完成后用一段话总结改动了哪些文件、新增了哪些视觉系统，并附上 §13 两份清单的自查结果。*
